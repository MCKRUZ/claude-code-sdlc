# Workshop Brief — ${CLIENT_NAME} Outcome Workshop
<!-- HARD RULES:
     1. The brief proper (everything above the appendices) fits on ONE PAGE. If an attendee
        can't read it in five minutes, it won't be read. Cut until it fits.
     2. Questions only. The brief never proposes outcomes, metrics, or solutions. Corpus
        framings appear only as attributed claims: "DOC-003 frames this as X — is that right?"
     3. Every claim carries a DOC-NNN reference. No orphan facts.
     4. This is a CURATED artifact: the discovery-analyst supplies candidates; the Pod Lead
        chooses what makes the page. Generate-and-send is the failure mode. -->

**When/where:** ${DATE_TIME_LOCATION} · **Runs:** ${DURATION}
**Attendees:** ${ATTENDEES_WITH_ROLES}
**Facilitator:** ${POD_LEAD_NAME} · **Pre-reading:** this page only

## What we read

${TOTAL_DOCUMENTS} documents provided by your team (full registry in Appendix C). The
load-bearing ones: ${TOP_3_5_DOCS_WITH_IDS_AND_ONE_CLAUSE_EACH}

## What the documents say

<!-- 3-6 factual claims that frame the working session. Claims, not conclusions. -->

- ${CLAIM} (${DOC_REF})
- ${CLAIM} (${DOC_REF})

## Where the documents disagree

<!-- The top contradictions by severity — usually 3-5. Each ends in its resolving question.
     Full detail in Appendix B. -->

1. ${DOC_REF_A} says ${X}; ${DOC_REF_B} says ${Y}. **${RESOLVING_QUESTION}** (CON-${NN})
2. ${...}

## What nobody has written down

<!-- 8-12 questions max on the page, grouped by agenda block. Full list in Appendix A.
     Only questions routed "workshop" — pre-workshop questions were already emailed. -->

- **Problem:** ${QUESTION} (Q-${NN})
- **Success metric:** ${QUESTION} (Q-${NN})
- **Constraints:** ${QUESTION} (Q-${NN})

## Decisions we need from the room

<!-- The must-leave-with list, 3-5 items. If the workshop produces only these, it succeeded. -->

1. The one success metric — and the named system it will be read from
2. The product-owner commitment: a named PO at the agreed hours, or proxy mode
3. ${ENGAGEMENT_SPECIFIC_DECISION}

## Agenda

| Block | Time | Output |
|-------|------|--------|
| The problem, in your words | 45 min | Problem narrative, current state, who hurts and how much |
| The three outcomes | 45 min | Business, software, capability — one sentence each |
| The one metric | 30 min | The metric, its baseline (or the admission nobody knows it), its source system |
| Constraints | 30 min | What's fixed, what's negotiable |
| Product ownership | 20 min | Named PO with hours, or proxy mode acknowledged |
| Tooling status | 10 min | Procurement status, who unblocks it, fallback date |

---

## Appendix A: Full question list

<!-- Inline question-list.md content or link to it -->

## Appendix B: Contradiction detail

<!-- Inline contradiction-list.md content or link to it -->

## Appendix C: Document registry

<!-- Inline document-registry.md index table or link to it -->
